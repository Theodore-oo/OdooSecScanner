import hashlib
import pickle

def bad_func():
    hashlib.md5(b'test')
    cr.execute('SELECT * FROM users WHERE name = %s' % name)
    pickle.loads(data)
    try:
        pass
    except Exception:
        pass

